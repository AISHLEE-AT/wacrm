const fs = require('fs'); const path = require('path'); function search(dir) { const files = fs.readdirSync(dir); for (const file of files) { const full = path.join(dir, file); if (fs.statSync(full).isDirectory()) search(full); else if (full.endsWith('.js')) { const content = fs.readFileSync(full, 'utf8'); if (content.match(/\{[\sA-Za-z0-9_,]*auth[\sA-Za-z0-9_,]*\}\s*=\s*[a-zA-Z0-9_]+/)) { console.log('MATCH IN:', full); const match = content.match(/.{0,30}\{[\sA-Za-z0-9_,]*auth[\sA-Za-z0-9_,]*\}\s*=\s*[a-zA-Z0-9_]+.{0,30}/); console.log(match[0]); } } } } search('.next/static');
