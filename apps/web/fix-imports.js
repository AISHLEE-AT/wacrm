const fs = require('fs'); const path = require('path'); function walk(dir) { const files = fs.readdirSync(dir); for (const file of files) { const full = path.join(dir, file); if (fs.statSync(full).isDirectory()) walk(full); else if (full.endsWith('.tsx') || full.endsWith('.jsx') || full.endsWith('.js') || full.endsWith('.ts')) { let content = fs.readFileSync(full, 'utf8'); if (content.includes('import { supabase } from') && content.includes('supabaseClient')) { content = content.replace(/import\s*\{\s*supabase\s*\}\s*from\s*['\x22].*?supabaseClient.*?['\x22];?/, 'import { createClient } from \x22@/lib/supabase/client\x22;\nconst supabase = createClient();'); fs.writeFileSync(full, content); console.log('Fixed', full); } } } } walk('src');
